# Canonical SSE Streams

Placeholder for v0.1.0. The mock CLIs (`crates/mock-cli-*`) currently
synthesize streams in-process; the v0.2.0 release will move the canonical
byte-for-byte SSE captures here so plugin authors can diff parser output
without rebuilding the mocks.
